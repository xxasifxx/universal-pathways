# Home page copy fixes

1. Replace the hero sentence in `src/routes/index.tsx` (lines 93-96) with a single line: "Putting Students First".
2. Rewrite the "Students First" platform highlight text in `src/lib/campaign.ts` from "Students take the courses they are ready for, whatever one grade says." to plain, direct language.
3. Verify the build and preview at mobile and desktop widths.
