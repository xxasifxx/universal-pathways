/**
 * Site-wide kill switch.
 *
 * While true, every route renders a plain 404 page: no public pages,
 * no admin, no canvassing, no review room. Set to false to bring the
 * site back exactly as it was.
 */
export const SITE_OFFLINE = true;
